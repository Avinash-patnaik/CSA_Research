# Build React app
FROM node:18-alpine AS build
WORKDIR /app

COPY frontend/CSA-chatbot/package*.json ./
RUN npm install
COPY frontend/CSA-chatbot/ .
RUN npm run build

# Serve with Nginx
FROM nginx:1.25-alpine
COPY --from=build /app/dist /usr/share/nginx/html

# Point to where your nginx.conf is actually located
COPY frontend/nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 5173
CMD ["nginx", "-g", "daemon off;"]