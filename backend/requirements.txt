fastapi[all]==0.115.2               # API framework (pydantic v2 compatible)
uvicorn[standard]==0.29.0           # ASGI server
mysql-connector-python==8.2.0       # MySQL client for Python 3.12+
python-dotenv==1.0.1                # For reading .env config
openai==1.30.1                      # Latest OpenAI client (new API format)
chromadb==0.4.24                    # Vector DB for similarity search
sentence-transformers==2.2.2        # Embeddings (e.g., all-MiniLM)
openai-whisper                      # Whisper model for video/audio transcription
tiktoken==0.7.0                     # Tokenizer for OpenAI models
pydantic==2.11.7                    # Pydantic v2 for FastAPI compatibility
huggingface-hub                     # For free Serverless Inference API
sentence-transformers==2.2.2        # Still useful for local embedding logic if needed
tiktoken==0.7.0                     # Tokenizer for OpenAI models
langchain-community
langchain-huggingface
pypdf